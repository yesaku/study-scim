apiVersion: v1
kind: Service
metadata:
  name: {{ include "study-scim.fullname" . }}
  labels:
    {{- include "study-scim.labels" . | nindent 4 }}
spec:
  type: {{ .Values.app.service.type }}
  ports:
    - port: {{ .Values.app.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "study-scim.selectorLabels" . | nindent 4 }}
